# Intentionally empty. A re-export would make this package a single import node, so
# importing one submodule would run the whole package and close cycles the split exists
# to avoid. Guarded by test_no_rules_package_reexports.
